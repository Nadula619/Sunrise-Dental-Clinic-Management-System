# Sunrise Dental Clinic Management System Configuration
server.port=8080

# Database Engine: mysql (WAMP / phpMyAdmin)
db.type=mysql

# MySQL Settings (WAMP Server on port 3306 with password 'root')
mysql.url=jdbc:mysql://localhost:3306/sunrise_dental_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
mysql.user=root
mysql.password=root

# MongoDB Settings (Optional Fallback)
mongodb.uri=mongodb://localhost:27017
mongodb.database=sunrise_dental_db

app.name=Sunrise Dental Clinic Management System
